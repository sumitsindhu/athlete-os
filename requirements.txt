# Web API
fastapi>=0.115.0,<1
uvicorn[standard]>=0.32.0,<1

# Config & validation (FastAPI is built on Pydantic v2)
pydantic>=2.10.0,<3
pydantic-settings>=2.6.0,<3

# Agent stack (keep langchain-* versions compatible; upgrade together)
langchain>=0.3.14,<0.4
langchain-core>=0.3.28,<0.4
langchain-openai>=0.3.0,<0.4
langchain-anthropic>=0.3.0,<0.4
langgraph>=0.2.60,<0.3

# Env files for local dev (Docker can use env vars directly)
python-dotenv>=1.0.0,<2
httpx>=0.28.0,<1
jupyter>=1.1.0,<2
ipykernel>=6.29.0,<7
